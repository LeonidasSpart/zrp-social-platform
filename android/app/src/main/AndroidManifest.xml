<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- allowBackup="false": this WebView loads the live, authenticated
         zrp.one session (see capacitor.config.ts - server.url), so its
         cookie jar/local storage IS the session. android-native's own
         manifest already made this exact fix (see its identical
         comment) for the token it stores locally; this Capacitor shell
         is the one actually published today, so a backup of its WebView
         data would let a physically-accessible, unlocked device's
         session be extracted via `adb backup` (or restored via Android's
         Auto Backup to another device) without ever touching the
         httpOnly cookie the browser itself protects. -->
    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera/mic: photo & video capture for posts/stories/messages, and
         voice/video calls. Media library: choosing existing photos/videos
         to share. READ_EXTERNAL_STORAGE is capped to pre-Android-13
         devices - Android 13+ uses READ_MEDIA_IMAGES/VIDEO instead, and
         the modern system Photo Picker generally needs no runtime
         permission at all. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
</manifest>
